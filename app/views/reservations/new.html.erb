<h class="title">予約内容確認</h>
 
 <%= form_with model:@reservation do |f| %>
  <table class="table is-hoverable">
   <tr>
    <h4 class="subtitle">¥<%= @room.price %>/日</h4>
   </tr>

   <tr>
    <th>開始日:</th>
    <td><%= @reservation&.start_at&.strftime("%Y年%m月%d日") %></td>
   </tr>  
    
   <tr>
    <th>終了日:</th>
    <td><%= @reservation&.end_at&.strftime("%Y年%m月%d日") %></td>
   </tr>

   <tr>
    <th>使用日数:</th>
    <td><%= @reservation.sum_of_days.to_i %>日</td>
   </tr> 
    
   <tr>
    <th>人数:</th>
    <td><%= @reservation.number %>人</td>
   </tr>
   
   <tr>
    <th>合計金額</th>
    <td><%= @reservation.sum_of_price %>円</td>
   </tr>
  </table>

   <p>上記内容でよろしければ、「予約確定」を押してください</p>
  
    <%= f.hidden_field :member_id, value: current_member.id %>
    <%= f.hidden_field :room_id, value:@room.id %>
    <%= f.hidden_field :number, value:@reservation.number %>
    <%= f.hidden_field :start_at %>
    <%= f.hidden_field :end_at %>


   <tr>
    <td>
     <button class="button is-info">予約を確定</button><br>
    </td>
   </tr>
  
   <tr>
    <td>
     <%= link_to"ルーム詳細に戻る",room_path(@room) %>
    <td>
   </tr>

 <% end %>
