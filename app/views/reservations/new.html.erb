<h3>予約内容確認</h3>
 
<%= form_with model:@reservation,url:new_reservation_path(@room), local: true do |f| %>
    
    <tr>
     <h4>¥<%= @room.price %>/日</h4>
    </tr>

    <tr>
     <th>開始日:</th>
     <td><%= @reservation.start_at.to_s %></td>
    </tr>
      
    <tr>
     <th>終了日:</th>
     <td><%= @reservation.end_at.to_s %></td>
    
    <tr>
     <th>使用日数:</th>
     <td><%= @reservation.sum_of_days %></td>
    </tr> 
    
    <tr>
     <th>人数:</th>
     <td><%= @reservation.number.to_i %>人</td>
    </tr>
   
    <tr>
     <th>合計金額</th>
    </tr>

  <p>上記内容でよろしければ、「予約確定」を押してください</p>
  
    <%= f.hidden_field :member_id, value: current_member.id %>
    <%= f.hidden_field :room_id, value:@reservation.room.id %>
    <%= f.hidden_field :number, value:@reservation.number %>
    <%= f.hidden_field :start_at %>
    <%= f.hidden_field :end_at %>


 
 <tr>
  <td>
   <%= f.submit "予約を確定" %>
  </td>
 </tr>
 <tr>
  <td>
   <%= link_to"ルーム詳細に戻る",room_path(@room) %>
  <td>
 </tr>
 <% end %>