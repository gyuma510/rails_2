<h2>登録済みルーム一覧</h2>

 <table>
  <thead>
   <th>ルーム画像</th>
   <th>ルーム名</th>
   <th>ルーム紹介</th>
   <th>料金</th>
   <th>作成日</th>
  </th>
  </thead>

  <tbody>
   <% @rooms.each do |room| %>
    <tr>
     <td>
      <%= link_to image_tag room.room_img_url.to_s %>
     </td>
     <td><%= link_to room.name, room %></td>
     <td><%= room.introduction %></td>
     <td><%= room.price %></td>
     <td><%= room.created_at.strftime("%Y年%m月%d日%H時%M分") %></td>
    </tr>
   <% end %>
  </tbody>
 </table>